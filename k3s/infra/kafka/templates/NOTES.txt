1. External Secrets Operator must be installed and a SecretStore for Infisical must exist.
2. Ensure Infisical contains the keys at the path configured in values.yaml.
3. Install chart: helm install kafka . -n kafka --create-namespace