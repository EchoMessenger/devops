apiVersion: v1
kind: Service
metadata:
  name: {{ include "router.fullname" . }}
spec:
  selector:
    app: {{ include "router.name" . }}
  ports:
    - name: http
      port: 7911
      targetPort: {{ .Values.service.port }}
