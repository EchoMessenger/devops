# Performance-test override for Router during backend pipeline measurements.
# Replicas and limits intentionally stay inherited from values.yaml.

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"

log:
  level: info
