apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinode-webapp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tinode-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinode-webapp.selectorLabels" . | nindent 4 }}