---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: webapp
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-webapp
  namespace: webapp
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: webapp
  policyTypes: [Ingress, Egress]
  ingress:
    # От Traefik
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - port: 8080
    # Healthcheck от kubelet
    - from:
        - ipBlock:
            cidr: 10.42.0.0/16  # Pod CIDR - для kube-probe
      ports:
        - port: 8080
  egress:
    # К Tinode API
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: tinode
      ports:
        - port: 6060
        - port: 16060
    # DNS
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP