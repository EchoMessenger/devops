# Performance-test override for first-login warm-up and Tinode auth mapping.
# Replicas and limits intentionally stay inherited from values.yaml.

resources:
  requests:
    cpu: 100m
    memory: 128Mi
