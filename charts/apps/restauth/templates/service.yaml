apiVersion: v1
kind: Service
metadata:
  name: {{ include "restauth.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "restauth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "restauth.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
