{{- if .Values.openbao.enabled }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ghcr-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tinode.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.openbao.clusterSecretStore }}
    kind: ClusterSecretStore

  target:
    name: ghcr-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "ghcr.io": {
                "username": "{{ "{{" }} .username {{ "}}" }}",
                "password": "{{ "{{" }} .token {{ "}}" }}",
                "auth": "{{ "{{" }} printf "%s:%s" .username .token | b64enc {{ "}}" }}"
              }
            }
          }

  data:
    - secretKey: username
      remoteRef:
        key: {{ .Values.openbao.secrets.registry.path }}
        property: username
    - secretKey: token
      remoteRef:
        key: {{ .Values.openbao.secrets.registry.path }}
        property: token

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "tinode.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tinode.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.openbao.clusterSecretStore }}
    kind: ClusterSecretStore

  target:
    name: {{ include "tinode.fullname" . }}
    creationPolicy: Owner
    deletionPolicy: Retain

  data:
    # PostgreSQL credentials
    - secretKey: postgres-username
      remoteRef:
        key: {{ .Values.openbao.secrets.postgres.path }}
        property: username
    - secretKey: postgres-password
      remoteRef:
        key: {{ .Values.openbao.secrets.postgres.path }}
        property: password
    - secretKey: postgres-database
      remoteRef:
        key: {{ .Values.openbao.secrets.postgres.path }}
        property: database
    - secretKey: postgres-dsn
      remoteRef:
        key: {{ .Values.openbao.secrets.postgres.path }}
        property: tinodeUrl
    
    # Encryption keys
    - secretKey: api-key-salt
      remoteRef:
        key: {{ .Values.openbao.secrets.keygen.path }}
        property: hmac_salt
    - secretKey: uid-encryption-key
      remoteRef:
        key: {{ .Values.openbao.secrets.keygen.path }}
        property: uid_key
    - secretKey: auth-token-key
      remoteRef:
        key: {{ .Values.openbao.secrets.keygen.path }}
        property: token_key
    
    {{- if .Values.email.smtp.password }}
    # SMTP password (optional, если хранится в OpenBao)
    - secretKey: smtp-password
      remoteRef:
        key: {{ .Values.openbao.secrets.smtp.path | default "tinode/smtp" }}
        property: password
    {{- end }}
    
  {{- if (eq .Values.media.handler "s3") }}
    {{- $s3Path := "tinode/s3" -}}
    {{- if and .Values.openbao.secrets.s3 .Values.openbao.secrets.s3.path }}
      {{- $s3Path = .Values.openbao.secrets.s3.path -}}
    {{- end }}
    - secretKey: aws-access-key-id
      remoteRef:
        key: {{ $s3Path }}
        property: accessKey
    - secretKey: aws-secret-access-key
      remoteRef:
        key: {{ $s3Path }}
        property: secretKey
    - secretKey: s3-endpoint
      remoteRef:
        key: {{ $s3Path }}
        property: endpoint
    - secretKey: s3-region
      remoteRef:
        key: {{ $s3Path }}
        property: region
    - secretKey: s3-bucket
      remoteRef:
        key: {{ $s3Path }}
        property: bucket
  {{- end }}
    
    {{- if .Values.tnpg.enabled }}
    # TNPG auth token (optional, если хранится в OpenBao)
    - secretKey: tnpg-auth-token
      remoteRef:
        key: {{ .Values.openbao.secrets.tnpg.path | default "tinode/tnpg" }}
        property: auth_token
    {{- end }}
{{- end }}