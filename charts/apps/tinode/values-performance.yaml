# Performance-test override for production-like backend measurements.
# Replicas and limits intentionally stay inherited from values.yaml.

resources:
  requests:
    cpu: 500m
    memory: 768Mi

plugins:
  router:
    timeout: 30000

extraEnv:
  - name: PERF_PROFILE
    value: "backend-pipeline"
