replicaCount: 1

image:
  repository: postgres
  tag: "16"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5432

storage:
  size: 5Gi
  storageClassName: local-path

postgres:
  user: restauth
  database: restauth
  dataDir: /var/lib/postgresql/data/pgdata

openbao:
  enabled: true
  secretStoreName: openbao-global
  secretPath: restauth/database
  refreshInterval: "1h"
  deletionPolicy: Retain

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "1Gi"
    cpu: "1000m"

nameOverride: ""
fullnameOverride: ""
